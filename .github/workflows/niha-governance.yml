name: niha Governance

on:
  pull_request:
    branches: [main]

jobs:
  governance:
    name: Governance Check
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v4

      - name: Install niha CLI
        run: npm install -g @niha-and-co/niha

      - name: Run niha governance
        uses: niha-and-co/ai-platform/.github/actions/niha-ci@v1
        with:
          api-key: ${{ secrets.NIHA_API_KEY }}
          workspace-id: ${{ vars.NIHA_WORKSPACE_ID }}
          api-url: ${{ vars.NIHA_PLATFORM_API_URL }}
          fail-on: blocked
          comment: "true"
